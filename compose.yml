version: '3.8'

services:
  proto-compiler:
    image: proto-compiler:latest
    container_name: proto-compiler
    command: "/bin/bash"
    stdin_open: true
    tty: true
    volumes:
      - ./proto:/app/proto

